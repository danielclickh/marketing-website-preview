'use client'
import {
  Dialog,
  Listbox,
  Popover as PopoverEncloser,
  Tab,
  Transition
} from '@headlessui/react'
const Popover = PopoverEncloser
const PopoverButton = Popover.Button
const PopoverGroup = Popover.Group
const PopoverPanel = Popover.Panel
export {
  Dialog,
  Listbox,
  Popover,
  Tab,
  Transition,
  PopoverButton,
  PopoverGroup,
  PopoverPanel
}
