name: Deploy

on:
  workflow_dispatch:
  push:
    branches: ["va-marketing-rewrite"]

jobs:
  deploy:
    name: Deploy
    runs-on: ubuntu-latest
    steps:
      - name: Call Vercel to redeploy
        run: |
          curl '${{ env.VERCEL_HOOK }}'
